using MFSystemInterface.Models.Registry;
using MFSystemInterface.Services.Utils;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Collections.Generic;

namespace MFSystemInterfaceTests.Models.Registry
{
    [TestClass()]
    public class RegKeyTests
    {
/*        [TestMethod()]
        public void GetTest()
        {
            var tests = FileUtil.ImportJson<ICollection<RegKey>>("Test\\Models\\Registry\\RegKey\\Get.json");
            foreach (var test in tests)
            {
                var result = test.Get();
                Assert.AreEqual(test, result, $"Expected:\n{test}\nActual:\n{result}");
            }
        }

        [TestMethod()]
        public void CheckTest()
        {
            var tests = FileUtil.ImportJson<ICollection<RegKey>>("Test\\Models\\Registry\\RegKey\\Check.json");
            foreach (var test in tests)
            {
                Assert.IsTrue(test.Check());
            }
        }*/
    }
}